// Auto-generated by generate_aot_kernels. Do not edit.
// Unit: esp_stokeslet_3d_ranges
#include <algorithm>
#include <array>
#include <dmk.h>
#include <dmk/types.hpp>
#include <dmk/vector_kernels.hpp>
#include <sctl.hpp>
#include <utility>
#include <vector>

namespace dmk {
constexpr int unroll_factor = 3;

template <class Real, int MaxVecLen>
residual_evaluator_range_func<Real> get_esp_stokeslet_3d_kernel_ranges(dmk_eval_type eval_level, int n_digits,
                                                                       const std::vector<std::vector<Real>> &coeffs) {
    constexpr int UF = unroll_factor;
    const int n = static_cast<int>(std::max(coeffs.at(0).size(), coeffs.at(1).size()));
    if (eval_level == DMK_VELOCITY) {
        if (n == 3) {
            constexpr int NC = 3;
            std::array<Real, 2 * NC> cf{};
            std::copy_n(coeffs[0].data(), coeffs[0].size(), cf.data());
            std::copy_n(coeffs[1].data(), coeffs[1].size(), cf.data() + NC);
            return [=](Real rsc, Real cen, Real d2max, Real thresh2, int n_src, const Real *r_src, const Real *charge,
                       const Real *normals, int n_ranges, const int *range_starts, const int *range_lens, int n_trg,
                       const Real *r_trg, Real *pot, const Real *q_trg, Real *pot_src) {
                stokeslet_3d_poly_all_pairs_ranges<Real, MaxVecLen, -1, NC, NC, DMK_VELOCITY>(
                    eval_level, n_digits, rsc, cen, d2max, thresh2, NC, NC, cf.data(), n_ranges, range_starts,
                    range_lens, n_src, r_src, charge, normals, n_trg, r_trg, pot, q_trg, pot_src, UF);
            };
        }
        if (n == 4) {
            constexpr int NC = 4;
            std::array<Real, 2 * NC> cf{};
            std::copy_n(coeffs[0].data(), coeffs[0].size(), cf.data());
            std::copy_n(coeffs[1].data(), coeffs[1].size(), cf.data() + NC);
            return [=](Real rsc, Real cen, Real d2max, Real thresh2, int n_src, const Real *r_src, const Real *charge,
                       const Real *normals, int n_ranges, const int *range_starts, const int *range_lens, int n_trg,
                       const Real *r_trg, Real *pot, const Real *q_trg, Real *pot_src) {
                stokeslet_3d_poly_all_pairs_ranges<Real, MaxVecLen, -1, NC, NC, DMK_VELOCITY>(
                    eval_level, n_digits, rsc, cen, d2max, thresh2, NC, NC, cf.data(), n_ranges, range_starts,
                    range_lens, n_src, r_src, charge, normals, n_trg, r_trg, pot, q_trg, pot_src, UF);
            };
        }
        if (n == 5) {
            constexpr int NC = 5;
            std::array<Real, 2 * NC> cf{};
            std::copy_n(coeffs[0].data(), coeffs[0].size(), cf.data());
            std::copy_n(coeffs[1].data(), coeffs[1].size(), cf.data() + NC);
            return [=](Real rsc, Real cen, Real d2max, Real thresh2, int n_src, const Real *r_src, const Real *charge,
                       const Real *normals, int n_ranges, const int *range_starts, const int *range_lens, int n_trg,
                       const Real *r_trg, Real *pot, const Real *q_trg, Real *pot_src) {
                stokeslet_3d_poly_all_pairs_ranges<Real, MaxVecLen, -1, NC, NC, DMK_VELOCITY>(
                    eval_level, n_digits, rsc, cen, d2max, thresh2, NC, NC, cf.data(), n_ranges, range_starts,
                    range_lens, n_src, r_src, charge, normals, n_trg, r_trg, pot, q_trg, pot_src, UF);
            };
        }
        if (n == 6) {
            constexpr int NC = 6;
            std::array<Real, 2 * NC> cf{};
            std::copy_n(coeffs[0].data(), coeffs[0].size(), cf.data());
            std::copy_n(coeffs[1].data(), coeffs[1].size(), cf.data() + NC);
            return [=](Real rsc, Real cen, Real d2max, Real thresh2, int n_src, const Real *r_src, const Real *charge,
                       const Real *normals, int n_ranges, const int *range_starts, const int *range_lens, int n_trg,
                       const Real *r_trg, Real *pot, const Real *q_trg, Real *pot_src) {
                stokeslet_3d_poly_all_pairs_ranges<Real, MaxVecLen, -1, NC, NC, DMK_VELOCITY>(
                    eval_level, n_digits, rsc, cen, d2max, thresh2, NC, NC, cf.data(), n_ranges, range_starts,
                    range_lens, n_src, r_src, charge, normals, n_trg, r_trg, pot, q_trg, pot_src, UF);
            };
        }
        if (n == 7) {
            constexpr int NC = 7;
            std::array<Real, 2 * NC> cf{};
            std::copy_n(coeffs[0].data(), coeffs[0].size(), cf.data());
            std::copy_n(coeffs[1].data(), coeffs[1].size(), cf.data() + NC);
            return [=](Real rsc, Real cen, Real d2max, Real thresh2, int n_src, const Real *r_src, const Real *charge,
                       const Real *normals, int n_ranges, const int *range_starts, const int *range_lens, int n_trg,
                       const Real *r_trg, Real *pot, const Real *q_trg, Real *pot_src) {
                stokeslet_3d_poly_all_pairs_ranges<Real, MaxVecLen, -1, NC, NC, DMK_VELOCITY>(
                    eval_level, n_digits, rsc, cen, d2max, thresh2, NC, NC, cf.data(), n_ranges, range_starts,
                    range_lens, n_src, r_src, charge, normals, n_trg, r_trg, pot, q_trg, pot_src, UF);
            };
        }
        if (n == 8) {
            constexpr int NC = 8;
            std::array<Real, 2 * NC> cf{};
            std::copy_n(coeffs[0].data(), coeffs[0].size(), cf.data());
            std::copy_n(coeffs[1].data(), coeffs[1].size(), cf.data() + NC);
            return [=](Real rsc, Real cen, Real d2max, Real thresh2, int n_src, const Real *r_src, const Real *charge,
                       const Real *normals, int n_ranges, const int *range_starts, const int *range_lens, int n_trg,
                       const Real *r_trg, Real *pot, const Real *q_trg, Real *pot_src) {
                stokeslet_3d_poly_all_pairs_ranges<Real, MaxVecLen, -1, NC, NC, DMK_VELOCITY>(
                    eval_level, n_digits, rsc, cen, d2max, thresh2, NC, NC, cf.data(), n_ranges, range_starts,
                    range_lens, n_src, r_src, charge, normals, n_trg, r_trg, pot, q_trg, pot_src, UF);
            };
        }
        if (n == 9) {
            constexpr int NC = 9;
            std::array<Real, 2 * NC> cf{};
            std::copy_n(coeffs[0].data(), coeffs[0].size(), cf.data());
            std::copy_n(coeffs[1].data(), coeffs[1].size(), cf.data() + NC);
            return [=](Real rsc, Real cen, Real d2max, Real thresh2, int n_src, const Real *r_src, const Real *charge,
                       const Real *normals, int n_ranges, const int *range_starts, const int *range_lens, int n_trg,
                       const Real *r_trg, Real *pot, const Real *q_trg, Real *pot_src) {
                stokeslet_3d_poly_all_pairs_ranges<Real, MaxVecLen, -1, NC, NC, DMK_VELOCITY>(
                    eval_level, n_digits, rsc, cen, d2max, thresh2, NC, NC, cf.data(), n_ranges, range_starts,
                    range_lens, n_src, r_src, charge, normals, n_trg, r_trg, pot, q_trg, pot_src, UF);
            };
        }
        if (n == 10) {
            constexpr int NC = 10;
            std::array<Real, 2 * NC> cf{};
            std::copy_n(coeffs[0].data(), coeffs[0].size(), cf.data());
            std::copy_n(coeffs[1].data(), coeffs[1].size(), cf.data() + NC);
            return [=](Real rsc, Real cen, Real d2max, Real thresh2, int n_src, const Real *r_src, const Real *charge,
                       const Real *normals, int n_ranges, const int *range_starts, const int *range_lens, int n_trg,
                       const Real *r_trg, Real *pot, const Real *q_trg, Real *pot_src) {
                stokeslet_3d_poly_all_pairs_ranges<Real, MaxVecLen, -1, NC, NC, DMK_VELOCITY>(
                    eval_level, n_digits, rsc, cen, d2max, thresh2, NC, NC, cf.data(), n_ranges, range_starts,
                    range_lens, n_src, r_src, charge, normals, n_trg, r_trg, pot, q_trg, pot_src, UF);
            };
        }
        if (n == 11) {
            constexpr int NC = 11;
            std::array<Real, 2 * NC> cf{};
            std::copy_n(coeffs[0].data(), coeffs[0].size(), cf.data());
            std::copy_n(coeffs[1].data(), coeffs[1].size(), cf.data() + NC);
            return [=](Real rsc, Real cen, Real d2max, Real thresh2, int n_src, const Real *r_src, const Real *charge,
                       const Real *normals, int n_ranges, const int *range_starts, const int *range_lens, int n_trg,
                       const Real *r_trg, Real *pot, const Real *q_trg, Real *pot_src) {
                stokeslet_3d_poly_all_pairs_ranges<Real, MaxVecLen, -1, NC, NC, DMK_VELOCITY>(
                    eval_level, n_digits, rsc, cen, d2max, thresh2, NC, NC, cf.data(), n_ranges, range_starts,
                    range_lens, n_src, r_src, charge, normals, n_trg, r_trg, pot, q_trg, pot_src, UF);
            };
        }
        if (n == 12) {
            constexpr int NC = 12;
            std::array<Real, 2 * NC> cf{};
            std::copy_n(coeffs[0].data(), coeffs[0].size(), cf.data());
            std::copy_n(coeffs[1].data(), coeffs[1].size(), cf.data() + NC);
            return [=](Real rsc, Real cen, Real d2max, Real thresh2, int n_src, const Real *r_src, const Real *charge,
                       const Real *normals, int n_ranges, const int *range_starts, const int *range_lens, int n_trg,
                       const Real *r_trg, Real *pot, const Real *q_trg, Real *pot_src) {
                stokeslet_3d_poly_all_pairs_ranges<Real, MaxVecLen, -1, NC, NC, DMK_VELOCITY>(
                    eval_level, n_digits, rsc, cen, d2max, thresh2, NC, NC, cf.data(), n_ranges, range_starts,
                    range_lens, n_src, r_src, charge, normals, n_trg, r_trg, pot, q_trg, pot_src, UF);
            };
        }
        if (n == 13) {
            constexpr int NC = 13;
            std::array<Real, 2 * NC> cf{};
            std::copy_n(coeffs[0].data(), coeffs[0].size(), cf.data());
            std::copy_n(coeffs[1].data(), coeffs[1].size(), cf.data() + NC);
            return [=](Real rsc, Real cen, Real d2max, Real thresh2, int n_src, const Real *r_src, const Real *charge,
                       const Real *normals, int n_ranges, const int *range_starts, const int *range_lens, int n_trg,
                       const Real *r_trg, Real *pot, const Real *q_trg, Real *pot_src) {
                stokeslet_3d_poly_all_pairs_ranges<Real, MaxVecLen, -1, NC, NC, DMK_VELOCITY>(
                    eval_level, n_digits, rsc, cen, d2max, thresh2, NC, NC, cf.data(), n_ranges, range_starts,
                    range_lens, n_src, r_src, charge, normals, n_trg, r_trg, pot, q_trg, pot_src, UF);
            };
        }
        if (n == 14) {
            constexpr int NC = 14;
            std::array<Real, 2 * NC> cf{};
            std::copy_n(coeffs[0].data(), coeffs[0].size(), cf.data());
            std::copy_n(coeffs[1].data(), coeffs[1].size(), cf.data() + NC);
            return [=](Real rsc, Real cen, Real d2max, Real thresh2, int n_src, const Real *r_src, const Real *charge,
                       const Real *normals, int n_ranges, const int *range_starts, const int *range_lens, int n_trg,
                       const Real *r_trg, Real *pot, const Real *q_trg, Real *pot_src) {
                stokeslet_3d_poly_all_pairs_ranges<Real, MaxVecLen, -1, NC, NC, DMK_VELOCITY>(
                    eval_level, n_digits, rsc, cen, d2max, thresh2, NC, NC, cf.data(), n_ranges, range_starts,
                    range_lens, n_src, r_src, charge, normals, n_trg, r_trg, pot, q_trg, pot_src, UF);
            };
        }
        if (n == 15) {
            constexpr int NC = 15;
            std::array<Real, 2 * NC> cf{};
            std::copy_n(coeffs[0].data(), coeffs[0].size(), cf.data());
            std::copy_n(coeffs[1].data(), coeffs[1].size(), cf.data() + NC);
            return [=](Real rsc, Real cen, Real d2max, Real thresh2, int n_src, const Real *r_src, const Real *charge,
                       const Real *normals, int n_ranges, const int *range_starts, const int *range_lens, int n_trg,
                       const Real *r_trg, Real *pot, const Real *q_trg, Real *pot_src) {
                stokeslet_3d_poly_all_pairs_ranges<Real, MaxVecLen, -1, NC, NC, DMK_VELOCITY>(
                    eval_level, n_digits, rsc, cen, d2max, thresh2, NC, NC, cf.data(), n_ranges, range_starts,
                    range_lens, n_src, r_src, charge, normals, n_trg, r_trg, pot, q_trg, pot_src, UF);
            };
        }
        if (n == 16) {
            constexpr int NC = 16;
            std::array<Real, 2 * NC> cf{};
            std::copy_n(coeffs[0].data(), coeffs[0].size(), cf.data());
            std::copy_n(coeffs[1].data(), coeffs[1].size(), cf.data() + NC);
            return [=](Real rsc, Real cen, Real d2max, Real thresh2, int n_src, const Real *r_src, const Real *charge,
                       const Real *normals, int n_ranges, const int *range_starts, const int *range_lens, int n_trg,
                       const Real *r_trg, Real *pot, const Real *q_trg, Real *pot_src) {
                stokeslet_3d_poly_all_pairs_ranges<Real, MaxVecLen, -1, NC, NC, DMK_VELOCITY>(
                    eval_level, n_digits, rsc, cen, d2max, thresh2, NC, NC, cf.data(), n_ranges, range_starts,
                    range_lens, n_src, r_src, charge, normals, n_trg, r_trg, pot, q_trg, pot_src, UF);
            };
        }
        if (n == 17) {
            constexpr int NC = 17;
            std::array<Real, 2 * NC> cf{};
            std::copy_n(coeffs[0].data(), coeffs[0].size(), cf.data());
            std::copy_n(coeffs[1].data(), coeffs[1].size(), cf.data() + NC);
            return [=](Real rsc, Real cen, Real d2max, Real thresh2, int n_src, const Real *r_src, const Real *charge,
                       const Real *normals, int n_ranges, const int *range_starts, const int *range_lens, int n_trg,
                       const Real *r_trg, Real *pot, const Real *q_trg, Real *pot_src) {
                stokeslet_3d_poly_all_pairs_ranges<Real, MaxVecLen, -1, NC, NC, DMK_VELOCITY>(
                    eval_level, n_digits, rsc, cen, d2max, thresh2, NC, NC, cf.data(), n_ranges, range_starts,
                    range_lens, n_src, r_src, charge, normals, n_trg, r_trg, pot, q_trg, pot_src, UF);
            };
        }
        if (n == 18) {
            constexpr int NC = 18;
            std::array<Real, 2 * NC> cf{};
            std::copy_n(coeffs[0].data(), coeffs[0].size(), cf.data());
            std::copy_n(coeffs[1].data(), coeffs[1].size(), cf.data() + NC);
            return [=](Real rsc, Real cen, Real d2max, Real thresh2, int n_src, const Real *r_src, const Real *charge,
                       const Real *normals, int n_ranges, const int *range_starts, const int *range_lens, int n_trg,
                       const Real *r_trg, Real *pot, const Real *q_trg, Real *pot_src) {
                stokeslet_3d_poly_all_pairs_ranges<Real, MaxVecLen, -1, NC, NC, DMK_VELOCITY>(
                    eval_level, n_digits, rsc, cen, d2max, thresh2, NC, NC, cf.data(), n_ranges, range_starts,
                    range_lens, n_src, r_src, charge, normals, n_trg, r_trg, pot, q_trg, pot_src, UF);
            };
        }
        if (n == 19) {
            constexpr int NC = 19;
            std::array<Real, 2 * NC> cf{};
            std::copy_n(coeffs[0].data(), coeffs[0].size(), cf.data());
            std::copy_n(coeffs[1].data(), coeffs[1].size(), cf.data() + NC);
            return [=](Real rsc, Real cen, Real d2max, Real thresh2, int n_src, const Real *r_src, const Real *charge,
                       const Real *normals, int n_ranges, const int *range_starts, const int *range_lens, int n_trg,
                       const Real *r_trg, Real *pot, const Real *q_trg, Real *pot_src) {
                stokeslet_3d_poly_all_pairs_ranges<Real, MaxVecLen, -1, NC, NC, DMK_VELOCITY>(
                    eval_level, n_digits, rsc, cen, d2max, thresh2, NC, NC, cf.data(), n_ranges, range_starts,
                    range_lens, n_src, r_src, charge, normals, n_trg, r_trg, pot, q_trg, pot_src, UF);
            };
        }
        if (n == 20) {
            constexpr int NC = 20;
            std::array<Real, 2 * NC> cf{};
            std::copy_n(coeffs[0].data(), coeffs[0].size(), cf.data());
            std::copy_n(coeffs[1].data(), coeffs[1].size(), cf.data() + NC);
            return [=](Real rsc, Real cen, Real d2max, Real thresh2, int n_src, const Real *r_src, const Real *charge,
                       const Real *normals, int n_ranges, const int *range_starts, const int *range_lens, int n_trg,
                       const Real *r_trg, Real *pot, const Real *q_trg, Real *pot_src) {
                stokeslet_3d_poly_all_pairs_ranges<Real, MaxVecLen, -1, NC, NC, DMK_VELOCITY>(
                    eval_level, n_digits, rsc, cen, d2max, thresh2, NC, NC, cf.data(), n_ranges, range_starts,
                    range_lens, n_src, r_src, charge, normals, n_trg, r_trg, pot, q_trg, pot_src, UF);
            };
        }
        if (n == 21) {
            constexpr int NC = 21;
            std::array<Real, 2 * NC> cf{};
            std::copy_n(coeffs[0].data(), coeffs[0].size(), cf.data());
            std::copy_n(coeffs[1].data(), coeffs[1].size(), cf.data() + NC);
            return [=](Real rsc, Real cen, Real d2max, Real thresh2, int n_src, const Real *r_src, const Real *charge,
                       const Real *normals, int n_ranges, const int *range_starts, const int *range_lens, int n_trg,
                       const Real *r_trg, Real *pot, const Real *q_trg, Real *pot_src) {
                stokeslet_3d_poly_all_pairs_ranges<Real, MaxVecLen, -1, NC, NC, DMK_VELOCITY>(
                    eval_level, n_digits, rsc, cen, d2max, thresh2, NC, NC, cf.data(), n_ranges, range_starts,
                    range_lens, n_src, r_src, charge, normals, n_trg, r_trg, pot, q_trg, pot_src, UF);
            };
        }
        if (n == 22) {
            constexpr int NC = 22;
            std::array<Real, 2 * NC> cf{};
            std::copy_n(coeffs[0].data(), coeffs[0].size(), cf.data());
            std::copy_n(coeffs[1].data(), coeffs[1].size(), cf.data() + NC);
            return [=](Real rsc, Real cen, Real d2max, Real thresh2, int n_src, const Real *r_src, const Real *charge,
                       const Real *normals, int n_ranges, const int *range_starts, const int *range_lens, int n_trg,
                       const Real *r_trg, Real *pot, const Real *q_trg, Real *pot_src) {
                stokeslet_3d_poly_all_pairs_ranges<Real, MaxVecLen, -1, NC, NC, DMK_VELOCITY>(
                    eval_level, n_digits, rsc, cen, d2max, thresh2, NC, NC, cf.data(), n_ranges, range_starts,
                    range_lens, n_src, r_src, charge, normals, n_trg, r_trg, pot, q_trg, pot_src, UF);
            };
        }
        if (n == 23) {
            constexpr int NC = 23;
            std::array<Real, 2 * NC> cf{};
            std::copy_n(coeffs[0].data(), coeffs[0].size(), cf.data());
            std::copy_n(coeffs[1].data(), coeffs[1].size(), cf.data() + NC);
            return [=](Real rsc, Real cen, Real d2max, Real thresh2, int n_src, const Real *r_src, const Real *charge,
                       const Real *normals, int n_ranges, const int *range_starts, const int *range_lens, int n_trg,
                       const Real *r_trg, Real *pot, const Real *q_trg, Real *pot_src) {
                stokeslet_3d_poly_all_pairs_ranges<Real, MaxVecLen, -1, NC, NC, DMK_VELOCITY>(
                    eval_level, n_digits, rsc, cen, d2max, thresh2, NC, NC, cf.data(), n_ranges, range_starts,
                    range_lens, n_src, r_src, charge, normals, n_trg, r_trg, pot, q_trg, pot_src, UF);
            };
        }
        if (n == 24) {
            constexpr int NC = 24;
            std::array<Real, 2 * NC> cf{};
            std::copy_n(coeffs[0].data(), coeffs[0].size(), cf.data());
            std::copy_n(coeffs[1].data(), coeffs[1].size(), cf.data() + NC);
            return [=](Real rsc, Real cen, Real d2max, Real thresh2, int n_src, const Real *r_src, const Real *charge,
                       const Real *normals, int n_ranges, const int *range_starts, const int *range_lens, int n_trg,
                       const Real *r_trg, Real *pot, const Real *q_trg, Real *pot_src) {
                stokeslet_3d_poly_all_pairs_ranges<Real, MaxVecLen, -1, NC, NC, DMK_VELOCITY>(
                    eval_level, n_digits, rsc, cen, d2max, thresh2, NC, NC, cf.data(), n_ranges, range_starts,
                    range_lens, n_src, r_src, charge, normals, n_trg, r_trg, pot, q_trg, pot_src, UF);
            };
        }
        if (n == 25) {
            constexpr int NC = 25;
            std::array<Real, 2 * NC> cf{};
            std::copy_n(coeffs[0].data(), coeffs[0].size(), cf.data());
            std::copy_n(coeffs[1].data(), coeffs[1].size(), cf.data() + NC);
            return [=](Real rsc, Real cen, Real d2max, Real thresh2, int n_src, const Real *r_src, const Real *charge,
                       const Real *normals, int n_ranges, const int *range_starts, const int *range_lens, int n_trg,
                       const Real *r_trg, Real *pot, const Real *q_trg, Real *pot_src) {
                stokeslet_3d_poly_all_pairs_ranges<Real, MaxVecLen, -1, NC, NC, DMK_VELOCITY>(
                    eval_level, n_digits, rsc, cen, d2max, thresh2, NC, NC, cf.data(), n_ranges, range_starts,
                    range_lens, n_src, r_src, charge, normals, n_trg, r_trg, pot, q_trg, pot_src, UF);
            };
        }
        if (n == 26) {
            constexpr int NC = 26;
            std::array<Real, 2 * NC> cf{};
            std::copy_n(coeffs[0].data(), coeffs[0].size(), cf.data());
            std::copy_n(coeffs[1].data(), coeffs[1].size(), cf.data() + NC);
            return [=](Real rsc, Real cen, Real d2max, Real thresh2, int n_src, const Real *r_src, const Real *charge,
                       const Real *normals, int n_ranges, const int *range_starts, const int *range_lens, int n_trg,
                       const Real *r_trg, Real *pot, const Real *q_trg, Real *pot_src) {
                stokeslet_3d_poly_all_pairs_ranges<Real, MaxVecLen, -1, NC, NC, DMK_VELOCITY>(
                    eval_level, n_digits, rsc, cen, d2max, thresh2, NC, NC, cf.data(), n_ranges, range_starts,
                    range_lens, n_src, r_src, charge, normals, n_trg, r_trg, pot, q_trg, pot_src, UF);
            };
        }
        if (n == 27) {
            constexpr int NC = 27;
            std::array<Real, 2 * NC> cf{};
            std::copy_n(coeffs[0].data(), coeffs[0].size(), cf.data());
            std::copy_n(coeffs[1].data(), coeffs[1].size(), cf.data() + NC);
            return [=](Real rsc, Real cen, Real d2max, Real thresh2, int n_src, const Real *r_src, const Real *charge,
                       const Real *normals, int n_ranges, const int *range_starts, const int *range_lens, int n_trg,
                       const Real *r_trg, Real *pot, const Real *q_trg, Real *pot_src) {
                stokeslet_3d_poly_all_pairs_ranges<Real, MaxVecLen, -1, NC, NC, DMK_VELOCITY>(
                    eval_level, n_digits, rsc, cen, d2max, thresh2, NC, NC, cf.data(), n_ranges, range_starts,
                    range_lens, n_src, r_src, charge, normals, n_trg, r_trg, pot, q_trg, pot_src, UF);
            };
        }
        if (n == 28) {
            constexpr int NC = 28;
            std::array<Real, 2 * NC> cf{};
            std::copy_n(coeffs[0].data(), coeffs[0].size(), cf.data());
            std::copy_n(coeffs[1].data(), coeffs[1].size(), cf.data() + NC);
            return [=](Real rsc, Real cen, Real d2max, Real thresh2, int n_src, const Real *r_src, const Real *charge,
                       const Real *normals, int n_ranges, const int *range_starts, const int *range_lens, int n_trg,
                       const Real *r_trg, Real *pot, const Real *q_trg, Real *pot_src) {
                stokeslet_3d_poly_all_pairs_ranges<Real, MaxVecLen, -1, NC, NC, DMK_VELOCITY>(
                    eval_level, n_digits, rsc, cen, d2max, thresh2, NC, NC, cf.data(), n_ranges, range_starts,
                    range_lens, n_src, r_src, charge, normals, n_trg, r_trg, pot, q_trg, pot_src, UF);
            };
        }
        if (n == 29) {
            constexpr int NC = 29;
            std::array<Real, 2 * NC> cf{};
            std::copy_n(coeffs[0].data(), coeffs[0].size(), cf.data());
            std::copy_n(coeffs[1].data(), coeffs[1].size(), cf.data() + NC);
            return [=](Real rsc, Real cen, Real d2max, Real thresh2, int n_src, const Real *r_src, const Real *charge,
                       const Real *normals, int n_ranges, const int *range_starts, const int *range_lens, int n_trg,
                       const Real *r_trg, Real *pot, const Real *q_trg, Real *pot_src) {
                stokeslet_3d_poly_all_pairs_ranges<Real, MaxVecLen, -1, NC, NC, DMK_VELOCITY>(
                    eval_level, n_digits, rsc, cen, d2max, thresh2, NC, NC, cf.data(), n_ranges, range_starts,
                    range_lens, n_src, r_src, charge, normals, n_trg, r_trg, pot, q_trg, pot_src, UF);
            };
        }
        if (n == 30) {
            constexpr int NC = 30;
            std::array<Real, 2 * NC> cf{};
            std::copy_n(coeffs[0].data(), coeffs[0].size(), cf.data());
            std::copy_n(coeffs[1].data(), coeffs[1].size(), cf.data() + NC);
            return [=](Real rsc, Real cen, Real d2max, Real thresh2, int n_src, const Real *r_src, const Real *charge,
                       const Real *normals, int n_ranges, const int *range_starts, const int *range_lens, int n_trg,
                       const Real *r_trg, Real *pot, const Real *q_trg, Real *pot_src) {
                stokeslet_3d_poly_all_pairs_ranges<Real, MaxVecLen, -1, NC, NC, DMK_VELOCITY>(
                    eval_level, n_digits, rsc, cen, d2max, thresh2, NC, NC, cf.data(), n_ranges, range_starts,
                    range_lens, n_src, r_src, charge, normals, n_trg, r_trg, pot, q_trg, pot_src, UF);
            };
        }
        if (n == 31) {
            constexpr int NC = 31;
            std::array<Real, 2 * NC> cf{};
            std::copy_n(coeffs[0].data(), coeffs[0].size(), cf.data());
            std::copy_n(coeffs[1].data(), coeffs[1].size(), cf.data() + NC);
            return [=](Real rsc, Real cen, Real d2max, Real thresh2, int n_src, const Real *r_src, const Real *charge,
                       const Real *normals, int n_ranges, const int *range_starts, const int *range_lens, int n_trg,
                       const Real *r_trg, Real *pot, const Real *q_trg, Real *pot_src) {
                stokeslet_3d_poly_all_pairs_ranges<Real, MaxVecLen, -1, NC, NC, DMK_VELOCITY>(
                    eval_level, n_digits, rsc, cen, d2max, thresh2, NC, NC, cf.data(), n_ranges, range_starts,
                    range_lens, n_src, r_src, charge, normals, n_trg, r_trg, pot, q_trg, pot_src, UF);
            };
        }
    }
    throw std::runtime_error("Unsupported eval_level, or n_coeffs outside the AOT range");
}

// Explicit instantiations
template residual_evaluator_range_func<float>
get_esp_stokeslet_3d_kernel_ranges<float, sctl::DefaultVecLen<float>()>(dmk_eval_type, int,
                                                                        const std::vector<std::vector<float>> &);
template residual_evaluator_range_func<double>
get_esp_stokeslet_3d_kernel_ranges<double, sctl::DefaultVecLen<double>()>(dmk_eval_type, int,
                                                                          const std::vector<std::vector<double>> &);

} // namespace dmk
